import styled from "styled-components";
import {AppBar} from "@mui/material";

export const StyledTopBar = styled(AppBar)`
    color: #4B424A!important;
    box-shadow: 0 2px 4px -1px rgba(0,0,0,0.1), 0 4px 5px 0 rgba(0,0,0,0.04), 0 1px 10px 0 rgba(0,0,0,0.02)!important;
`